name: Deploy DEV

run-name: Deployment DEV - snapshot

on:
  workflow_run:
    workflows:
      - Integration Build
    types:
      - completed

  workflow_dispatch:
    inputs:
      source_run_id:
        description: "Run ID del workflow Integration Build que generó el snapshot"
        required: true
        type: string

permissions:
  contents: read
  pages: write
  id-token: write
  actions: read

concurrency:
  group: pages-dev
  cancel-in-progress: true

jobs:
  deploy-dev:
    name: Deployment DEV from snapshot
    runs-on: ubuntu-latest

    if: >
      github.event_name == 'workflow_dispatch' ||
      (
        github.event.workflow_run.conclusion == 'success' &&
        (
          github.event.workflow_run.head_branch == 'main' ||
          github.event.workflow_run.head_branch == 'develop'
        )
      )

    environment:
      name: dev
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Resolve source run id
        id: source
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            echo "run_id=${{ inputs.source_run_id }}" >> "$GITHUB_OUTPUT"
          else
            echo "run_id=${{ github.event.workflow_run.id }}" >> "$GITHUB_OUTPUT"
          fi

      - name: Show source run id
        run: |
          echo "Using Integration Build run id:"
          echo "${{ steps.source.outputs.run_id }}"

      - name: Download DEV snapshot artifact
        uses: actions/download-artifact@v4
        with:
          run-id: ${{ steps.source.outputs.run_id }}
          github-token: ${{ secrets.GITHUB_TOKEN }}
          pattern: deployment-dev-snapshot-*
          path: downloaded
          merge-multiple: true

      - name: Show downloaded artifact content
        run: |
          echo "Downloaded content:"
          find downloaded -maxdepth 4 -type f | sort

      - name: Validate downloaded artifact
        run: |
          test -f downloaded/VERSION
          test -f downloaded/ARTIFACT_NAME
          test -f downloaded/BUILD_VERSION
          test -f downloaded/BUILD_COMMIT
          test -f downloaded/BUILD_TIMESTAMP
          test -f downloaded/web/index.html
          test -f downloaded/web/styles.css
          test -f downloaded/web/script.js
          test -d downloaded/web/assets

      - name: Validate DEV uses snapshot version
        run: |
          VERSION=$(cat downloaded/VERSION)
          if [[ "$VERSION" != *"-SNAPSHOT" ]]; then
            echo "DEV deploys must use a SNAPSHOT version"
            exit 1
          fi

      - name: Prepare GitHub Pages site
        run: |
          mkdir -p site

          cp downloaded/web/index.html site/
          cp downloaded/web/styles.css site/
          cp downloaded/web/script.js site/
          cp -r downloaded/web/assets site/
          cp downloaded/VERSION site/

          echo "Deployment DEV" > site/DEPLOYMENT_NAME.txt
          echo "Environment: DEV" > site/DEPLOYMENT_INFO.txt
          echo "Artifact: $(cat downloaded/ARTIFACT_NAME)" >> site/DEPLOYMENT_INFO.txt
          echo "Version: $(cat downloaded/VERSION)" >> site/DEPLOYMENT_INFO.txt
          echo "Snapshot timestamp: $(cat downloaded/BUILD_TIMESTAMP)" >> site/DEPLOYMENT_INFO.txt
          echo "Commit: $(cat downloaded/BUILD_COMMIT)" >> site/DEPLOYMENT_INFO.txt
          echo "Source Integration run id: ${{ steps.source.outputs.run_id }}" >> site/DEPLOYMENT_INFO.txt

      - name: Configure GitHub Pages
        uses: actions/configure-pages@v5

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: site

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4